import styled from 'styled-components';

const AboutPage = () => {
    return (
        <AboutPageStyles>
            <h2>Om</h2>
        </AboutPageStyles>
    );
};

const AboutPageStyles = styled.div``;

export default AboutPage;
